export type LambdaData = {
  region: string;
  functionName: string;
  codeSize: number;
  description: string;
  lastModifiedDate: string;
  runtime: string;
  memory: number;
  state: string;
  lastUpdateStatus: string;
  lastUpdateStatusReason: string;
};
